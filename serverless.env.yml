---
etl_bucket:
  dev: &etl_bucket vw-data-lab-etl

redshift:
  core:
    dev: &redshift-core aqs-core.*.eu-west-1.redshift.amazonaws.com

ssm_kms_key:
  dev: &ssm_kms_key arn:aws:kms:${self:provider.region}:${self:provider.account}:key/
